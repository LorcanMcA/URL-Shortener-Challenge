@model URL_Shortner_Challenge.Models.ShortLink

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ShortLink</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="entered" class="control-label"></label>
                <input asp-for="entered" class="form-control" />
                <span asp-validation-for="entered" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
    <div class="col-md-4">
        <p class="lead my-3">This link will be permanant and can be viewed on the links page.</p>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
